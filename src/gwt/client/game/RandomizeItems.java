package gwt.client.game;

import java.util.Map;

import gwt.shared.datamodel.VParams;

public class RandomizeItems extends VParams{

	// add a range of plusses or minuses, don't touch items with the special tag
	//add abilities (haste, strength), add resistances
	//if a weapon add brandings
	
	//this could be combined with a low drop rate for a highly varied field.
	@Override
	public void execute(Map<String, Object> map) {
		// TODO Auto-generated method stub
		super.execute(map);
	}
}
